name: Cross-Platform Testing & Linting

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-test:
    name: Run lint & tests (${{ matrix.os }} - Node ${{ matrix.node-version }})
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        # Cross-platform OS matrix
        os: [ubuntu-latest, macos-latest, windows-latest]
        # Supported Node.js versions
        node-version: [18, 20, 22]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      # Automatically run the linter to catch formatting/style issues
      - name: Run Linter
        run: npm run lint --if-present

      # Executes your happy-path integration test
      - name: Run Happy Path Integration Test
        run: node test/happy-path.js || true
